on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: Clone Submodules
        shell: bash
        run: |
          auth_header="$(git config --local --get http.https://github.com/.extraheader)"
          git submodule sync --recursive
          git -c "http.extraheader=$auth_header" -c protocol.version=2 submodule update --init --force --recursive --depth=1

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2.2.2
        with:
          hugo-version: '0.58.3'

      - name: Build
        run: hugo


      - uses: actions/checkout@v2
        with:
          repository: zmaillard/zmaillard.github.io
          token: ${{ secrets.GITHUB_REPO_TOKEN }}
          path: zmaillard.github.io

      - name: Filesystem
        run: du -al
#
#      - run: |
#          version=`echo "${{ env.IMAGE_VERSION }}"`
#          sed -i "s/\(go\-bot\:\)\([0-9]*\.[0-9]*\.[0-9]*\)/\1$version/" k8s.sagebrushgis/overlay/prod/gobot-dep.yaml
#
#      - uses: peter-evans/create-pull-request@v2
#        with:
#          title: "Updating Go-Bot To ${{ env.IMAGE_VERSION }}"
#          token: ${{ secrets.GITHUB_REPO_TOKEN }}
#          path: k8s.sagebrushgis
#          branch-suffix: short-commit-hash